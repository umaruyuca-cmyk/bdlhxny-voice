# 公开镜像:public/ 已不含私有运行台目录。
FROM alpine:3.20 AS prune
WORKDIR /src
COPY public/ ./public/

FROM nginx:1.27-alpine
COPY nginx.conf /etc/nginx/conf.d/default.conf
COPY --from=prune /src/public/ /usr/share/nginx/html/
EXPOSE 8082
