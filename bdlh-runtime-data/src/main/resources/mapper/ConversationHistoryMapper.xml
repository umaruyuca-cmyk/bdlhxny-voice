<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bdlh.runtime.mapper.ConversationHistoryMapper">

    <!-- 归档会话：messages 显式指定 JsonbTypeHandler，保证以 jsonb 类型落库 -->
    <insert id="insertArchive" parameterType="com.bdlh.runtime.entity.ConversationHistory"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO public.conversation_history (user_id, session_id, messages, summary, token_count, created_at)
        VALUES (#{userId}, #{sessionId},
                #{messages, typeHandler=com.bdlh.runtime.handler.JsonbTypeHandler},
                #{summary}, #{tokenCount}, NOW())
    </insert>

    <!-- 参数化加载最近归档，防止 limit 字符串拼接并保持数据库侧截断。 -->
    <select id="selectRecent" resultType="com.bdlh.runtime.entity.ConversationHistory">
        SELECT id, user_id, session_id, summary, token_count, created_at
        FROM public.conversation_history
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

</mapper>
