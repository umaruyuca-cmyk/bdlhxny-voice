# Prompt 09 · 项目去金融化交付说明

> 日期：2026-08-25  
> 分支：`touchstone`  
> 状态：代码与测试已落地；**未** commit / push；**未**执行任何 SQL。

## 1. 金融痕迹清单（三层）与处置

| 层 | 原痕迹 | 处置 |
|---|---|---|
| A · 默认偏好 | `SCENE_TOOLSETS` 默认含 market/portfolio/research/watch；system 提示 C-1/C-2；`is_trading_semantic` 硬编码；output C1/C2 默认词表；BASELINE「金融分析助手」 | 默认仅 `general`；提示词改为危险动作/不当结论；危险动作注册表（默认可空）；C1/C2 改 live 词表 + 场景包注入；BASELINE 改为工具型助手 |
| A · 护栏 | `DefaultResponseGuardrail` 内置买入/卖出词 | 默认可配置空词表；测试显式注入 |
| B · 测试 | 多处默认假定金融场景装载 | `finance_pack` fixture / `_run` 内 `enable_scenario_pack("finance")` |
| C · 场景包 | 无独立开关 | 新增 `bdlh_runtime.scenarios` + `scenarios/finance`；`docs/scenarios/finance/README.md`；SQL 标记脚本（不代跑） |
| 残留（有意保留） | `observations/normalizer`、`executor` 对 `market.*`/`portfolio.*` 能力名的适配；`fastpath_data` 禁止意图样例；registry 测试种子中的能力行 | 运行时兼容 DB 已登记工具名；禁止样例用于分流评测；种子归测试资产，非默认产品文案 |

## 2. 主要改动文件

- 新增：`engine/src/bdlh_runtime/scenarios/{__init__,dangerous_actions,finance/__init__}.py`
- 新增：`docs/scenarios/finance/README.md`、`engine/var/cases/scenarios/finance/README.md`
- 新增：`db/postgresql/changes/20260826-scenario-pack-finance-marker.sql`（**未执行**）
- 核心：`engine/loader.py`、`tools/catalog.py`、`output_guardrail.py`、`prompts/*.md`、`baseline_agent.py`、`guardrails/policies.py`
- 对比用例：`comparison_cases_data.py`、`tool_catalog_snapshot.py` 去掉默认金融 distractor
- 测试：`conftest.py` 的 `finance_pack`；engine/eval/tools 相关用例
- 展示：`web/public/showcase-data/cases.json`、`tools.json` 默认文案中性化

## 3. 护栏泛化前后对照

| 能力 | 改造前 | 改造后 |
|---|---|---|
| 工具注册拦截 | `is_trading_semantic` + 固定中英词表 | `DangerousActionProfile` 注册表；默认空；`finance` 包加载交易档案 |
| 出口 C1/C2 | 类内写死关键词 + 「投资建议」页脚 | `append_default_output_check` + `set_live_compliance_keywords`；默认空 |
| 响应护栏 | `_TRADING_TERMS` 类常量 | 构造参数 `blocked_terms`（默认空） |
| 兼容别名 | — | `is_trading_semantic` / `C1ComplianceCheck` 仍可导入 |

## 4. 场景包启用

```bash
SCENARIO_PACKS=finance
```

或：

```python
from bdlh_runtime.scenarios import enable_scenario_pack, disable_all_scenario_packs
enable_scenario_pack("finance")
```

详见 `docs/scenarios/finance/README.md`。

## 5. 验收记录

| 项 | 结果 |
|---|---|
| `pytest`（engine 全量） | **509 passed** |
| `session_cross_eval --compile-only` | **六项 PASS** |
| 危险动作：空词表可注册含 buy 描述 | `test_empty_dangerous_profile_allows_registration` / `test_catalog_allows_trading_wording_when_profile_empty` |
| 危险动作：finance 档案拦截 | `test_catalog_register_rejects_trading_semantic_card` 等 |
| 默认场景 | `test_scoped_mapping_core_is_general_only` → 仅 `general` |
| SQL | 未执行 |
| 真实 LLM / 正式实验 | 未跑 |

## 6. 中性演示用例说明

公开对比用例默认路径使用 `cmp-basic-notool-01` / `cmp-basic-deny-01`（档案越权，非持仓）与 `general` 场景工具；金融垂直用例需显式 `SCENARIO_PACKS=finance` 后再跑 routing / ab-eval 金融题库。
