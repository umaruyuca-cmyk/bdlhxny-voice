# M3 前置数据契约差距与实施状态

> TASK_PHASE: M3 user-facts-v2
> 执行 Prompt：v1.7 §10
> 回归基线：230 passed
> 当前状态：USER_FACTS_V2_DEVELOPMENT_COMPLETE / VALUATION_PENDING / RULE_ENGINE_BLOCKED / RELEASE_BLOCKED

## 1. 本轮允许范围

本轮继续只实现不依赖金融阈值的数据层：在已有严格契约、精确授权、Normalizer 与
fail-closed `FinancialSnapshotBuilder` 基础上，补齐 Java 用户事实 v2、受认证确认入口和
Python v2 元数据兼容。Finance Runtime 的
`SUITABILITY` 仍返回 `ACTION_NOT_ENABLED`，不会生成 `SUITABLE` 等生产结论。

## 2. 仓库事实与差距

| 目标字段 | 当前权威来源 | 仓库事实 | 本轮行为 |
|---|---|---|---|
| 持仓用户事实 | Java positions v2 | 已有 symbol/quantity/cost/target；新增 exchange/currency/source/confirmed_at/source_ref；旧记录不回填真实性 | USER_INPUT 仅经认证设置 API 确认为 USER_CONFIRMED |
| 当前 `market_value/weight_pct` | 标准行情 + PortfolioValuationBuilder | Java 不保存派生市值/实际权重；Builder 尚未实现 | 保持 None，Snapshot PARTIAL/LIMITED |
| `total_assets` | PortfolioValuationBuilder | 已确认 cash 可用；仍需完整持仓行情后计算本轮可投资资产 | 保持 None，不用 cash 冒充总资产 |
| `max_loss_tolerance_pct` | Java risk profile v2 | 已增加持久化、确认入口、只读 DTO 与 0..100 校验 | USER_CONFIRMED 正常提供；旧数据保持缺失 |
| `liquid_assets/near_term_cash_needs` | Java account v2 | 已增加显式金额、需求期限、确认来源与非负校验 | 不从 monthly_budget/cash_reserve_ratio 猜测 |
| Rule 阈值 | ADR-004 | 尚未批准 | 不实现可产生真实三类个性化结果的 Engine |

## 3. 解锁条件

1. 实现 `PortfolioValuationBuilder`，补齐标准行情 identity/currency/as_of，确定性计算
   market_value/weight_pct/本轮 total_assets，并覆盖缺行情、跨币种与动态预算。
2. ADR-004 的阈值、单位、等号边界、聚合规则获得业务/风险负责人批准。
3. M0 与 M4 Guardrail 发布门禁关闭前，即使 M3 开发完成也保持 RELEASE_BLOCKED。

## 4. 禁止的临时绕过

- 不把 `target_weight` 当作当前权重；
- 不用成本价计算当前市值；
- 不把 cash 当作 total_assets；
- 不把 cash_reserve_ratio 当作 max_loss_tolerance_pct；
- 不把“Java HTTP 查询成功”当作 LIVE；
- 不允许客户端提交 user_id/data_mode/source_type/confirmation_ref；
- 不把本轮派生市值、实际权重和 total_assets 写回为无时效 Java 配置；
- 不在代码中放示例阈值或“合理默认值”；
- 不启用默认 API、旧 Root Graph 或最终聊天个性化输出。

## 5. 本轮实施结果

- `FinancialDomainRequest` 已锁定 SUITABILITY 单标的 + comprehensive 研究配置；
- `SuitabilityAssessment` 已补充严格的 rule_set_version/rule_ids/evidence_refs；
- 三个最小用户 Capability 已增加 suitability Toolset 元数据与精确授权，交易历史仍排除；
- Application Finance Executor 已支持精确 Java 路由，但 SUITABILITY Runtime 尚未启用；
- 新增供应商无关用户数据 Normalizer，身份不一致稳定抛出 SNAPSHOT_IDENTITY_MISMATCH；
- 新增纯 `FinancialSnapshotBuilder`，captured_at 仅来自 provenance，相同输入稳定复现；
- 当前 Java 字段不足时 Snapshot 明确 PARTIAL/LIMITED，不伪造集中度、总资产、最大损失或流动性；
- 新增 `financial-user-data.v2` 数据库迁移：持仓 identity/currency/provenance、风险损失容忍、
  流动性、近期现金需求、profile_version 与确认审计；旧记录不回填 LIVE/USER_CONFIRMED；
- 新增独立认证设置 API：完整确认金融画像和完整替换持仓；使用 JWT user_id、
  `profile_version + Idempotency-Key`，服务端生成 confirmation_ref；不进入 Capability Registry；
- Java 内部 Data API 继续全部只读，并返回 schema_version/data_mode/source_type/query_status/
  data_time/confirmation_ref/missing_fields；SUCCESS 不再等于 LIVE；
- Python Normalizer 消费 v2 元数据；普通 java-api provenance 缺 v2 真实性标记时降为 UNAVAILABLE；
- Java 全量回归：`165 tests`，0 failure，0 error，2 skipped；
- Python 全量回归：`245 passed in 1.91s`。
