# 20260817 — SQL 字段注释整理

## 变更摘要

- 全部 `postgresql/schema/*.sql`、`bootstrap.sql` 补齐 `COMMENT ON TABLE/COLUMN`
- `seed/registry.sql` 按块整理排版
- `mysql/user_schema.sql` 补齐表/字段 `COMMENT`
- `runtime.run_registry.runtime_path` 默认值改为 `cognitive_finance`（与产品路径一致）

## 前置条件

- **空库全量**：按下方顺序重建（推荐）
- 若库已按上一份基线建好且仅需补注释：可单独重跑各 schema 中的 `COMMENT ON` 段，但最稳妥仍是空库全量

## 执行顺序

与 [20260817_空库全量初始化.md](./20260817_空库全量初始化.md) 相同：

```text
1. postgresql/bootstrap.sql
2. postgresql/schema/platform_contract.sql
3. postgresql/schema/runtime_core.sql
4. postgresql/schema/task_messaging.sql
5. postgresql/schema/notifications.sql
6. postgresql/schema/run_projection.sql
7. postgresql/schema/registry.sql
8. postgresql/schema/financial_user_data.sql
9. postgresql/schema/memory_service.sql
10. postgresql/seed/registry.sql
```

可选 MySQL：`mysql/user_schema.sql`

## 验收

- `\d+ runtime.chat_session`（或客户端查看）能看到字段注释
- Registry 八表与 seed 数据正常
- `run_registry.runtime_path` 默认值为 `cognitive_finance`
