# StockWise 部署环境变量（复制为 .env 并填实际值）
# 此文件不含敏感信息，可入库；.env 不要入库

# PostgreSQL（云服务器已有的容器，端口映射到宿主机 5432）
DATABASE_URL=jdbc:postgresql://localhost:5432/stockwise
PG_USER=root
PG_PASSWORD=改成你的PG密码

# Redis（云服务器已有的容器，端口映射到宿主机 6379）
REDIS_HOST=localhost

# Ollama（另一台电脑，内网穿透后的地址）
OLLAMA_URL=http://你的穿透地址:11434

# DeepSeek API
DEEPSEEK_API_KEY=sk-你的key
